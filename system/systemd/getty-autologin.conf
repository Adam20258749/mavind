# Live ISO only: autologin "mavind" on tty1, which runs mavind-session from
# ~/.bash_profile. The installer deletes this file so the installed system
# uses greetd instead.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin mavind --noclear %I $TERM
