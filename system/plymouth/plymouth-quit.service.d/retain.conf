# Keep the last splash frame (the "M") on screen after plymouth exits, so there
# is no black gap between the end of boot and labwc painting the desktop.
[Service]
ExecStart=
ExecStart=-/usr/bin/plymouth quit --retain-splash
