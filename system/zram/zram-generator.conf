# zram-generator: compressed RAM swap. On a 2 GB machine this roughly doubles
# usable memory for cold pages without touching disk.
[zram0]
zram-size = min(ram / 2, 3072)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
